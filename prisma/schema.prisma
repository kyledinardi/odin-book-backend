generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           Int       @id @default(autoincrement())
  username     String    @unique
  passwordHash String
  pfpUrl       String    @default("https://res.cloudinary.com/dhg8qxkfc/image/upload/v1722008094/fvgrcexpgvimfjfp3uj4.webp")
  bio          String?
  followers    User[]    @relation("followers")
  followed     User[]    @relation("followers")
  posts        Post[]    @relation("author")
  likedPosts   Post[]    @relation("likes")
  comments     Comment[]
}

model Post {
  id        Int       @id @default(autoincrement())
  text      String
  timestamp DateTime  @default(now())
  imageUrl  String
  author    User      @relation("author", fields: [userId], references: [id])
  userId    Int
  likes     User[]    @relation("likes")
  comments  Comment[]
}

model Comment {
  id        Int      @id @default(autoincrement())
  text      String
  timestamp DateTime @default(now())
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
  post      Post     @relation(fields: [postId], references: [id])
  postId    Int
}
